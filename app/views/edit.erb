<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
            <form action="/posts/<%= @post.id %>" method="POST">
            <input id="hidden" type="hidden" name="_method" value="patch">
            Name: <input id="name" type="text" value="<%= @post.name %>" name="post[name]"> <br>
            Content: <input id="content" type="text" value="<%= @post.content %>" name="post[content]"> <br>
            <input type="submit" value="submit">
        </form>
    </body>
</html>
